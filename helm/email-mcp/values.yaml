image:
  repository: REGISTRY/email-mcp  # replace with your registry after: podman build + push
  tag: latest

smtp:
  host: "smtp.example.com"
  port: "587"
  tls: "true"
  from: "agent@example.com"
  user: "CHANGE_ME"
  password: "CHANGE_ME"

route:
  host: "email-mcp.apps.CLUSTER_DOMAIN"
